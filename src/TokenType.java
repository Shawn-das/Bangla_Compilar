public enum TokenType {
    DHORO,      // ধরো
    ID,         // Identifiers (Bangla letters)
    NUMBER,     // ০-৯
    ASSIGN,     // =
    PLUS, MINUS, MUL, DIV,
    LPAREN, RPAREN,
    SEMI,       // ;
    EOF         // End of file
}